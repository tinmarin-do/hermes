# Secrets
.envrc
.env
*.key
*.pem

# GCP credentials (local)
.gcloud_config/

# Python
__pycache__/
*.pyc
.venv/
.uv/
*.egg-info/
dist/
.coverage
coverage.xml
htmlcov/

# Data lake (local DuckDB, models) — root-relative so it does NOT match src/data/
/data/
*.duckdb

# Terraform
terraform/.terraform/
terraform/*.tfplan
terraform/tfplan.binary
terraform/*.tfstate
terraform/*.tfstate.backup
terraform/.terraform.lock.hcl

# Docker
.docker/

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Claude Code — settings personales (heartbeat, etc.)
.claude/settings.local.json
src/dashboard/static/data/snapshot.json

# Graphify — grafo de conocimiento local (research, no versionado).
# Sin anclar: la herramienta escribe un graphify-out/ por raíz indexada
# (src/graphify-out/, docs/graphify-out/), no uno solo en la raíz del repo.
graphify-out/
