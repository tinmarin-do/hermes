{
  "role": "trading-agent",
  "description": "Analyst, trader, and portfolio manager roles within LangGraph pipeline.",
  "sub_roles": ["analyst-technical", "analyst-sentiment", "analyst-onchain", "trader", "portfolio-manager"],
  "input_schema": "RegimeSignal (Gold)",
  "output_schema": "AgentDecision",
  "allowed_tools": ["agents.debug", "agents.status"],
  "forbidden_tools": ["execution.*", "infra.*", "cost.*"],
  "constraints": [
    "Only operates on Gold-layer data",
    "Decisions are RECOMMENDATIONS only — risk-agent must approve before execution",
    "Must include confidence score and debate rationale in every decision"
  ]
}
