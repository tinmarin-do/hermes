{
  "role": "orchestrator",
  "description": "Coordinates the full Hermes pipeline. Calls other agents/tools in sequence.",
  "allowed_tools": ["cost.*", "agents.*", "data.*", "ops.*"],
  "forbidden_tools": ["execution.live"],
  "pipeline_sequence": [
    "data.validate-gold",
    "cost.gate (llm)",
    "agents.run",
    "execution.paper (if approved)"
  ],
  "decision_authority": "pipeline coordination only — no trading decisions"
}
